import AsyncStorage from "@react-native-async-storage/async-storage";
import { Stack, router } from "expo-router";
import { useEffect, useState } from "react";
import { Alert, KeyboardAvoidingView, Platform, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from "react-native";

import type { FirstConceptTemplate } from "@/lib/concept-templates";
import { CUSTOM_TEMPLATES_KEY, parseCustomTemplates, removeCustomTemplate, renameCustomTemplate } from "@/lib/custom-templates";

export default function TemplateLibraryScreen() {
  const [templates, setTemplates] = useState<FirstConceptTemplate[]>([]);
  const [editingId, setEditingId] = useState<string | null>(null);
  const [draftLabel, setDraftLabel] = useState("");

  useEffect(() => { AsyncStorage.getItem(CUSTOM_TEMPLATES_KEY).then((stored) => setTemplates(parseCustomTemplates(stored))).catch(() => undefined); }, []);
  const save = async (next: FirstConceptTemplate[]) => { setTemplates(next); await AsyncStorage.setItem(CUSTOM_TEMPLATES_KEY, JSON.stringify(next)); };
  const beginRename = (template: FirstConceptTemplate) => { setEditingId(template.id); setDraftLabel(template.label); };
  const commitRename = async (id: string) => { const next = renameCustomTemplate(templates, id, draftLabel); await save(next); setEditingId(null); };
  const confirmRemove = (template: FirstConceptTemplate) => Alert.alert("Delete saved template?", `“${template.label}” will be removed from this device.`, [{ text: "Cancel", style: "cancel" }, { text: "Delete", style: "destructive", onPress: () => void save(removeCustomTemplate(templates, template.id)) }]);

  return <KeyboardAvoidingView style={styles.screen} behavior={Platform.OS === "ios" ? "padding" : undefined}><Stack.Screen options={{ headerShown: false }} /><View style={styles.header}><Pressable onPress={() => router.back()} style={({ pressed }) => [styles.backButton, pressed && styles.pressed]}><Text style={styles.backText}>‹</Text></Pressable><View><Text style={styles.eyebrow}>LOCAL LIBRARY</Text><Text style={styles.headerTitle}>Saved templates</Text></View><View style={styles.headerSpace} /></View><ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled" showsVerticalScrollIndicator={false}><Text style={styles.title}>Keep your favorite starting points.</Text><Text style={styles.description}>These templates are stored only on this device and appear in the first-concept wizard whenever you create a new idea.</Text>{templates.length ? <View style={styles.list}>{templates.map((template) => <TemplateCard key={template.id} template={template} editing={editingId === template.id} draftLabel={draftLabel} onDraftChange={setDraftLabel} onBeginRename={() => beginRename(template)} onCommitRename={() => void commitRename(template.id)} onCancelRename={() => setEditingId(null)} onDelete={() => confirmRemove(template)} />)}</View> : <EmptyState />}</ScrollView><View style={styles.footer}><Pressable onPress={() => router.push("/first-concept-wizard")} style={({ pressed }) => [styles.primary, pressed && styles.pressed]}><Text style={styles.primaryText}>Create an idea with a template</Text><Text style={styles.arrow}>→</Text></Pressable></View></KeyboardAvoidingView>;
}

function TemplateCard({ template, editing, draftLabel, onDraftChange, onBeginRename, onCommitRename, onCancelRename, onDelete }: { template: FirstConceptTemplate; editing: boolean; draftLabel: string; onDraftChange: (value: string) => void; onBeginRename: () => void; onCommitRename: () => void; onCancelRename: () => void; onDelete: () => void }) { return <View style={styles.card}>{editing ? <View><Text style={styles.fieldLabel}>TEMPLATE NAME</Text><TextInput value={draftLabel} onChangeText={onDraftChange} autoFocus selectTextOnFocus returnKeyType="done" onSubmitEditing={onCommitRename} style={styles.renameInput} /><View style={styles.editActions}><Pressable onPress={onCancelRename} style={({ pressed }) => [styles.cancel, pressed && styles.pressed]}><Text style={styles.cancelText}>Cancel</Text></Pressable><Pressable disabled={!draftLabel.trim()} onPress={onCommitRename} style={({ pressed }) => [styles.save, pressed && styles.pressed, !draftLabel.trim() && styles.disabled]}><Text style={styles.saveText}>Save name</Text></Pressable></View></View> : <><View style={styles.cardHeader}><View style={styles.kindBadge}><Text style={styles.kindText}>{template.kind.toUpperCase()}</Text></View><View style={styles.cardActions}><Pressable onPress={onBeginRename} hitSlop={8} style={({ pressed }) => [styles.actionTextButton, pressed && styles.pressed]}><Text style={styles.renameText}>Rename</Text></Pressable><Pressable onPress={onDelete} hitSlop={8} style={({ pressed }) => [styles.actionTextButton, pressed && styles.pressed]}><Text style={styles.deleteText}>Delete</Text></Pressable></View></View><Text style={styles.templateLabel}>{template.label}</Text><Text style={styles.templateTitle}>{template.title}</Text><Text style={styles.templateNote} numberOfLines={3}>{template.note}</Text></>}</View>; }
function EmptyState() { return <View style={styles.empty}><Text style={styles.emptyMark}>◇</Text><Text style={styles.emptyTitle}>No saved templates yet.</Text><Text style={styles.emptyCopy}>When a note feels like a useful beginning, save it from the first-concept wizard to reuse it here.</Text></View>; }

const styles = StyleSheet.create({
  screen: { flex: 1, backgroundColor: "#0B1020", paddingTop: 56 }, header: { height: 57, paddingHorizontal: 20, flexDirection: "row", alignItems: "center", gap: 12 }, backButton: { width: 40, height: 40, borderRadius: 20, backgroundColor: "#151C2E", alignItems: "center", justifyContent: "center" }, backText: { color: "#F3F6FC", fontSize: 32, lineHeight: 34, marginTop: -3 }, eyebrow: { color: "#48D6E8", fontSize: 9, letterSpacing: 1.1, fontWeight: "900" }, headerTitle: { color: "#EAF0FC", fontSize: 14, fontWeight: "900", marginTop: 2 }, headerSpace: { flex: 1 }, content: { paddingHorizontal: 24, paddingTop: 30, paddingBottom: 30 }, title: { color: "#F3F6FC", fontSize: 27, lineHeight: 34, fontWeight: "800", letterSpacing: -0.6 }, description: { color: "#A8B5CB", fontSize: 14, lineHeight: 21, marginTop: 9 }, list: { gap: 11, marginTop: 24 }, card: { borderRadius: 18, padding: 15, backgroundColor: "#151C2E", borderWidth: 1, borderColor: "#2F415F" }, cardHeader: { flexDirection: "row", justifyContent: "space-between", alignItems: "center" }, kindBadge: { borderRadius: 8, paddingHorizontal: 8, paddingVertical: 5, backgroundColor: "#17343B", borderWidth: 1, borderColor: "#3D777D" }, kindText: { color: "#8AE1C2", fontSize: 9, letterSpacing: 0.8, fontWeight: "900" }, cardActions: { flexDirection: "row", gap: 13 }, actionTextButton: { paddingVertical: 5 }, renameText: { color: "#BDB6FF", fontSize: 11, fontWeight: "900" }, deleteText: { color: "#FFAC99", fontSize: 11, fontWeight: "900" }, templateLabel: { color: "#F3F6FC", fontSize: 17, fontWeight: "900", marginTop: 13 }, templateTitle: { color: "#BFC9DD", fontSize: 12, fontWeight: "800", marginTop: 4 }, templateNote: { color: "#91A0B9", fontSize: 12, lineHeight: 18, marginTop: 8 }, fieldLabel: { color: "#71809A", fontSize: 9, letterSpacing: 1, fontWeight: "900", marginBottom: 8 }, renameInput: { height: 49, borderRadius: 13, paddingHorizontal: 13, color: "#F3F6FC", fontSize: 15, fontWeight: "800", backgroundColor: "#0E1528", borderWidth: 1, borderColor: "#526680" }, editActions: { flexDirection: "row", justifyContent: "flex-end", gap: 9, marginTop: 11 }, cancel: { height: 37, paddingHorizontal: 13, borderRadius: 11, alignItems: "center", justifyContent: "center" }, cancelText: { color: "#A5B2CB", fontSize: 11, fontWeight: "900" }, save: { height: 37, paddingHorizontal: 13, borderRadius: 11, alignItems: "center", justifyContent: "center", backgroundColor: "#6258CE" }, saveText: { color: "#FFFFFF", fontSize: 11, fontWeight: "900" }, empty: { marginTop: 24, minHeight: 186, borderRadius: 19, padding: 20, alignItems: "center", justifyContent: "center", backgroundColor: "#121B30", borderWidth: 1, borderColor: "#293B5A" }, emptyMark: { color: "#72D5DF", fontSize: 31, fontWeight: "900" }, emptyTitle: { color: "#E8EDF8", fontSize: 16, fontWeight: "900", marginTop: 10 }, emptyCopy: { color: "#91A0B9", fontSize: 12, lineHeight: 18, textAlign: "center", marginTop: 6 }, footer: { borderTopWidth: 1, borderColor: "#202B44", paddingHorizontal: 20, paddingTop: 14, paddingBottom: 30 }, primary: { height: 53, borderRadius: 16, backgroundColor: "#7C6CFF", paddingHorizontal: 16, flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, primaryText: { color: "#FFFFFF", fontSize: 14, fontWeight: "900" }, arrow: { color: "#FFFFFF", fontSize: 21 }, pressed: { opacity: 0.7, transform: [{ scale: 0.98 }] }, disabled: { opacity: 0.45 },
});
